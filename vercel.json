{
    "version": 2,
    "buildCommand": "npx expo export -p web && mv dist/index.html dist/app.html && cp -r web-landing/* dist/ && mkdir -p dist/releases && (cp public/releases/presence-latest.apk dist/releases/presence-latest.apk 2>/dev/null || true)",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/app",
            "destination": "/app.html"
        },
        {
            "source": "/app/(.*)",
            "destination": "/app.html"
        }
    ],
    "headers": [
        {
            "source": "/releases/(.*).apk",
            "headers": [
                {
                    "key": "Content-Type",
                    "value": "application/vnd.android.package-archive"
                },
                {
                    "key": "Content-Disposition",
                    "value": "attachment; filename=\"presence.apk\""
                }
            ]
        }
    ]
}